const db = require("../config/db");

// Specific student logic can go here (e.g., getting student profile stats)
async function getStudentStats(studentId) {
    // ...
}

module.exports = { getStudentStats };
