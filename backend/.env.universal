# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tutor_hub
DB_PORT=3306

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET_KEY=your_secret_key_here

# Universal Email Configuration
# Gmail (for @gmail.com, @googlemail.com addresses)
GMAIL_USER=tutorhub.abhaysir@gmail.com
GMAIL_PASS=your-gmail-app-password

# Outlook (for @outlook.com, @hotmail.com, @live.com addresses)
OUTLOOK_USER=your-outlook@outlook.com
OUTLOOK_PASS=your-outlook-app-password

# Zoho Mail (for @zoho.com addresses)
ZOHO_USER=your-zoho@zoho.com
ZOHO_PASS=your-zoho-password

# SendGrid (universal fallback)
SENDGRID_API_KEY=your-sendgrid-api-key

FRONTEND_URL=http://localhost:4200
